{
  "name": "portfolio-ssr",
  "version": "1.0.0",
  "private": true,
  "description": "SSR wrapper for tapiwa.me: real security headers, true 301 canonical host redirect, custom 404",
  "scripts": {
    "build:bundle": "esbuild server/index.mjs --bundle --platform=node --target=node22 --format=cjs --outfile=dist-bundle/server.js && rm -rf dist-bundle/site && cp -r site dist-bundle/site",
    "test": "node test/server.test.mjs"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
